# Webz.io API token for the Topic Sentiment Analysis demo.
#
# Copy this file to `.env.local` and add a real token, free at
# https://app.webz.io. This template is safe to commit; `.env.local` is
# gitignored and must never be.
#
#   cp .env.example .env.local
#
WEBZ_API_TOKEN=
